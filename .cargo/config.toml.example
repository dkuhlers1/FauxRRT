# Optional local Cargo override. Copy to config.toml (gitignored) if this
# checkout lives on OneDrive and crate build scripts fail on a synced target/.
#
# [build]
# target-dir = "C:\\Users\\YOU\\AppData\\Local\\fauxrrt-cargo-target"
